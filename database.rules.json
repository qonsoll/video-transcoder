{
  "rules": {
    "translations": {
      ".read": "true",
      "$appName": {
        "$lang": {
          ".read": "true",
          ".write": "auth.uid != null",
          "$text":{
            ".validate": "newData.isString()"
          }
        }
      }
    }
  }
}
